## .github/workflows/jekyll.yml
#name: Build and Deploy Jekyll site to Pages
#
#on:
#  # Runs on pushes to ANY branch, providing continuous integration.
#  push:
#
#  # Allows you to run this workflow manually from the Actions tab.
#  workflow_dispatch:
#
## Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages.
#permissions:
#  contents: read
#  pages: write
#  id-token: write
#
## Allow only one concurrent deployment to avoid race conditions.
#concurrency:
#  group: "pages"
#  cancel-in-progress: false
#
#jobs:
#  # The 'build' job runs on every push to any branch. It acts as a
#  # safety check to ensure the site can be built successfully.
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Setup Ruby
#        uses: ruby/setup-ruby@v1
#        with:
#          bundler-cache: true # Caches gems for faster builds
#          cache-version: 0
#
#      - name: Setup Pages
#        id: pages
#        uses: actions/configure-pages@v5
#
#      - name: Build with Jekyll
#        # Uses the base_path output from the setup-pages step to build
#        # with the correct relative path for GitHub Pages.
#        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
#        env:
#          JEKYLL_ENV: production
#
#      - name: Upload artifact
#        # This step is conditional: it only runs on the 'main' branch.
#        # It packages the built site (_site folder) for the deploy job.
#        if: github.ref == 'refs/heads/main'
#        uses: actions/upload-pages-artifact@v3
#
#  # The 'deploy' job only runs on the 'main' branch after the 'build'
#  # job has successfully completed and uploaded an artifact.
#  deploy:
#    environment:
#      name: github-pages
#      url: ${{ steps.deployment.outputs.page_url }}
#    runs-on: ubuntu-latest
#    needs: build
#    if: github.ref == 'refs/heads/main'
#    steps:
#      - name: Deploy to GitHub Pages
#        id: deployment
#        uses: actions/deploy-pages@v4
